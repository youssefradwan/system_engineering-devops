#!/usr/bin/env bash
# A Bash script that stops the 4-to_infinity_and_beyond process without using kill or killall

# Find the PID(s) of the 4-to_infinity_and_beyond script and terminate using `pkill`
# `pkill -f` matches the full command line
pkill -f 4-to_infinity_and_beyond
